node_modules/
.DS_Store
*.log

# Keep the repo's own assets/ tracked despite the case-insensitive global /Assets/ ignore (macOS)
!/assets/

# Python bytecode and test caches (the gate and its suite are Python)
__pycache__/
*.py[cod]
.pytest_cache/
